<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- Head -->
  <title>
    <%= title %>
  </title>
  <link rel="stylesheet" href="/bootstrap/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="/@fortawesome/fontawesome-free/css/all.min.css">
  <link rel='stylesheet' href='/css/style.css' />
  <link rel="icon" type="image/x-icon" href="/img/favicon.ico">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/devicons/devicon@latest/devicon.min.css">

</head>

<body>
  <!-- Main navbar -->

  <nav class="navbar navbar-expand-lg navbar-dark bg-primary;">



    <!-- Brand/logo -->
    <a class="navbar-brand" href="/">
      <img src="../../public/img/TeamLogo.png" alt="logo" style="width:80px; margin-left: 20px;">
      Web Expert
    </a>


    <!-- Toggle button when view on smaller devices   -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <!-- Links -->
      <ul class="navbar-nav ms-auto mb-2 lg-0">
        <li class="nav-item">
          <% if(title=='Home') { %>
            <a class="nav-link active" href="/">
          <%} else { %>
            <a class="nav-link" href="/">
          <% } %>
            <i class="fas fa-lg fa-home"></i> Home</a>
        </li>
        <li class="nav-item dropdown">
          <% if (title=="Surveys" || title=="New Survey" || title=="Edit Survey" ) { %>
            <a class="nav-link active dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
          <% } else { %>
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
          <% } %>
            <i class="fas fa-lg fa-poll"></i> Surveys
            </a>
            <ul class="dropdown-menu">
              <li><a class="dropdown-item" href="/surveys/list">Available Surveys</a></li>
              <li><a class="dropdown-item" href="/surveys/add">New Survey</a></li>
            </ul>
        </li>
        <li class="nav-item">
          <% if(title=='Sign in') { %>
            <a class="nav-link active" href="/users/signin">
          <% } else { %>
            <a class="nav-link" href="/users/signin">
          <% } %>
            <i class="fas fa-lg fa-poll"></i> Sign in</a>
        </li>
        <!-- <li class="nav-item">
          <% if (typeof userName == 'string' && userName) { %>
            <a class="nav-link" href="/users/signout" onclick="return confirm('Are you sure?')">Sign Out (<%= userName %>)</a>
          <%} else { %>
            <a class="nav-link" href="/users/signin"><i class="fas fa-lock"></i>Sign in</a>
          <% } %>
        </li> -->
      </ul>
    </div>


  </nav>